on:
  push:
    branches: main

name: Hugo Publish

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: latest
          extended: true

      - name: Install Pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - name: Install Hugo Narrow
        run: git clone https://github.com/tom2almighty/hugo-narrow themes/hugo-narrow

      - name: Build citation-processed content
        run: python3 scripts/build_hugo_content.py

      - name: Build Hugo site
        run: hugo --minify --destination public --cleanDestinationDir

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
